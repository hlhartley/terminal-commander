import React from 'react';
import './CSS/main.scss';

const Header = (props) => {
    return (
        <div className="header">
           <p>header text</p>
        </div>
        )
}
export default Header;